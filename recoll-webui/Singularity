Bootstrap: docker
From: ubuntu:18.10

%labels
MAINTAINER TRF

%files

%environment
    
%post
    sudo apt-get update -y
    sudo apt-get install -y pwgen
    sudo apt-get install -y recoll git
    git clone https://framagit.org/medoc92/recollwebui.git
    cd recollwebui; ./configure; make; sudo make install
%runscript
    exec "$@"
